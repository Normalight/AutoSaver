<Window x:Class="AutoSaver.Views.AddEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="添加程序" Height="300" Width="320"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent">

    <Border Background="{DynamicResource WindowBackground}" BorderBrush="{DynamicResource DividerColor}" BorderThickness="1" CornerRadius="12">
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="添加程序" Style="{StaticResource H1}" Margin="0,0,0,16"/>

            <!-- Two action buttons -->
            <Grid Grid.Row="1" Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button x:Name="BrowseButton" Grid.Column="0"
                        Style="{StaticResource SecondaryButton}"
                        Click="OnBrowseClick">
                    <StackPanel Orientation="Horizontal">
                        <Path Data="M10 4H4c-1.1 0-2 .9-2 2v12c0 1.1.9 2 2 2h16c1.1 0 2-.9 2-2V8c0-1.1-.9-2-2-2h-8l-2-2z"
                              Stroke="{DynamicResource TextSecondary}" StrokeThickness="1.35"
                              Fill="Transparent" Width="16" Height="16" Stretch="Uniform" VerticalAlignment="Center"/>
                        <TextBlock Text="选择本地程序" Margin="8,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Button x:Name="PickRunningButton" Grid.Column="2"
                        Style="{StaticResource SecondaryButton}"
                        Click="OnPickRunningClick">
                    <StackPanel Orientation="Horizontal">
                        <Path Data="M19 3H5c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h14c1.1 0 2-.9 2-2V5c0-1.1-.9-2-2-2zm-5 14H7v-2h7v2zm3-4H7v-2h10v2zm0-4H7V7h10v2z"
                              Stroke="{DynamicResource TextSecondary}" StrokeThickness="1.2"
                              StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                              Width="16" Height="16" Stretch="Uniform" VerticalAlignment="Center"/>
                        <TextBlock Text="正在运行中选取" Margin="8,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </Grid>

            <!-- Form -->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="72"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="48"/>
                    <RowDefinition Height="48"/>
                    <RowDefinition Height="40"/>
                </Grid.RowDefinitions>

                <TextBlock Text="显示名称" VerticalAlignment="Center"
                           Style="{StaticResource Body}" Grid.Row="0" Grid.Column="0"/>
                <TextBox x:Name="NameBox" Grid.Row="0" Grid.Column="1" Height="34" VerticalAlignment="Center"/>

                <TextBlock Text="进程名" VerticalAlignment="Center"
                           Style="{StaticResource Body}" Grid.Row="1" Grid.Column="0"/>
                <TextBox x:Name="ExeBox" Grid.Row="1" Grid.Column="1" Height="34" VerticalAlignment="Center"/>

                <CheckBox x:Name="EnabledCheck" Content="启用"
                          Grid.Row="2" Grid.Column="1" VerticalAlignment="Center"/>
            </Grid>

            <!-- Action buttons -->
            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                <Button Content="取消" Style="{StaticResource GhostButton}"
                        Height="34" Width="76" IsCancel="True" Margin="0,0,10,0"/>
                <Button Content="确定" Style="{StaticResource PrimaryButton}"
                        Height="34" Width="76" IsDefault="True" Click="OnOkClick"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
